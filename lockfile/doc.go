// Package lockfile parses dependency lockfiles from multiple package ecosystems
// into a common format. Supported formats include npm (package-lock.json),
// Yarn (yarn.lock), pnpm (pnpm-lock.yaml), Go (go.sum), Cargo (Cargo.lock),
// pip (requirements.txt), Bundler (Gemfile.lock), and Poetry (poetry.lock).
package lockfile
